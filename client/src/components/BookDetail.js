import React from "react";

function BookDetail ({ book }) {
    const { id, img_url, title, author, description, pages, genre, likes, comments  } = book
    

    return (
        <div></div>
    )
}

export default BookDetail